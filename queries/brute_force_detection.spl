-- Figure 3: Brute Force Login Attempts on WidgetApp
-- Purpose: Detect repeated failed authentication attempts

index=final_project sourcetype=widgetapp user=BDRVLS
| search status="failed"
| stats count by src_ip user
| where count > 5
